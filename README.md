# 1GlobalTeste

Use de DockerCompose.yml inside the Teste GlobalRank1/

that will run the project , sql server and the MSSQLtool that should move the data and the database to the SQLSERVER

if you executing it from a Visual Studio , MSSQLTOOLS maybe doesn't work properly, in that case open the SQL server with a SQL SERVER MANEGEMENT STUDIO and execute the script.sql and then the inserts.sql. 

in the application that should open automatically, you can look the documentation on .;swagger;index.html.

inside that you have 2 version. they work with the database previously charged. 

ass well we have all the tests for the implementation working with a mock context
